# V2.2 人工操作手册（基于 v2.zip 实际代码核对）

适用项目目录：`F:\一二三\v2`

核对对象：`ppl_runner.py`、`ppl_engine/*`、`ppl_plan.yaml`、`ppl_rules.yaml`、`ppl_runner.db`、`alpha_results.db`、当前 `run_0002`。

## 0. 当前真实状态

从上传项目中的数据库读取到：

- run_id：`run_0002`
- run_profile：`PRODUCTION_RESEARCH`
- status/current_stage：`PAUSED`
- Selected Initial：72
- CACHE_COMPLETE：72
- NEW_SIMULATION_REQUIRED：0
- Deferred Repair Plans：23
- Repair Reserve：48
- Repair 已消费：0
- `alpha_results.db` 已完成 Initial Search

当前 `ppl_execution_plan.json` 显示：

- selected_candidates = 72
- cache_complete = 72
- new_simulation_required = 0
- resume_existing = 0
- retry_per_v21_policy = 0

## 1. 进入项目目录

PowerShell：

```powershell
Set-Location "F:\一二三\v2"
```

## 2. 查看 run_0002 状态

```powershell
python ppl_runner.py --status --run-id run_0002
```

这是本地读取，不访问 BRAIN，不产生 POST。

当前应看到 `run_profile=PRODUCTION_RESEARCH`、`status=PAUSED`。

## 3. 重建并查看 Summary

```powershell
python ppl_runner.py --summary --run-id run_0002 --rebuild-derived
```

作用：重建 Family / Priority 派生表并更新 `ppl_summary.json`。

网络请求：0。

注意：该命令会更新 Runner DB 中的派生表和 `ppl_summary.json`，但不会提交 Simulation。

只查看现有文件也可以：

```powershell
Get-Content .\ppl_summary.json -Raw
```

## 4. 查看 Family

```powershell
python ppl_runner.py --family-preview --run-id run_0002
```

网络请求：0，写入：0。

## 5. 查看 Priority

```powershell
python ppl_runner.py --priority-preview --run-id run_0002
```

网络请求：0，写入：0。

## 6. Reconcile 预览

先只预览，不写：

```powershell
python ppl_runner.py --reconcile --run-id run_0002 --preview
```

确认没有异常后再正式 Reconcile：

```powershell
python ppl_runner.py --reconcile --run-id run_0002
```

正式 Reconcile 只读取本地 `alpha_results.db` 与 `ppl_runner.db`，不会访问 BRAIN。

建议每次真实 Simulation/Repair 后都执行一次。

## 7. 查看 23 个 Deferred Repair Plan

当前 CLI 没有单独的 `--repair-list` 参数，使用只读 SQLite 查询。

PowerShell：

```powershell
@'
import sqlite3, json
c = sqlite3.connect(r"ppl_runner.db")
c.row_factory = sqlite3.Row
rows = c.execute("""
SELECT repair_plan_id,parent_candidate_id,target_failure,repair_type,
       plan_status,projected_new_posts,committed_posts,consumed_posts,
       candidate_spec_json
FROM ppl_repair_plans
WHERE run_id='run_0002'
ORDER BY created_at,repair_plan_id
""").fetchall()
for i,r in enumerate(rows,1):
    spec=json.loads(r['candidate_spec_json'])
    print(f"{i:02d} | {r['repair_plan_id']} | {r['repair_type']} | {r['plan_status']}")
    print("     parent:", r['parent_candidate_id'])
    print("     target:", r['target_failure'])
    print("     expr  :", spec.get('expression_preview'))
    print("     posts :", r['projected_new_posts'], 'committed=',r['committed_posts'],'consumed=',r['consumed_posts'])
c.close()
'@ | python -
```

## 8. 查看 Repair Plan 对应的原 Alpha 指标

```powershell
@'
import sqlite3, json
rdb=sqlite3.connect(r"ppl_runner.db"); rdb.row_factory=sqlite3.Row
adb=sqlite3.connect(r"alpha_results.db"); adb.row_factory=sqlite3.Row
plans=rdb.execute("SELECT * FROM ppl_repair_plans WHERE run_id='run_0002' ORDER BY created_at").fetchall()
for i,p in enumerate(plans,1):
    c=rdb.execute("SELECT * FROM ppl_candidates WHERE candidate_id=?",(p['parent_candidate_id'],)).fetchone()
    a=adb.execute("SELECT alpha_id,sharpe,fitness,turnover,returns,margin,status FROM alpha_results WHERE sim_key=?",(c['sim_key'],)).fetchone()
    spec=json.loads(p['candidate_spec_json'])
    print(f"{i:02d} {p['repair_plan_id']}")
    print('  Alpha    :', a['alpha_id'] if a else c['alpha_id'])
    print('  Sharpe   :', a['sharpe'] if a else None)
    print('  Fitness  :', a['fitness'] if a else None)
    print('  Turnover :', a['turnover'] if a else None)
    print('  Repair   :', p['repair_type'])
    print('  New expr :', spec.get('expression_preview'))
rdb.close(); adb.close()
'@ | python -
```

## 9. 当前 23 个 Repair Plan 中值得优先看的计划

根据当前数据库，自动生成的 23 个计划主要是两类：

- `TURNOVER_HIGH_TS_MEAN_2`
- `REVERSE_DIRECTION`

其中原始信号较强、值得优先观察的包括：

1. `rplan_cd29e095040791e915f1e14a`
   - Alpha `ak1m7YR2`
   - Sharpe 3.18
   - Fitness 1.48
   - Turnover 0.9584
   - Repair：`ts_mean(predicted_first_quantile_one_day_return_5, 2)`

2. `rplan_70f977d26af0e1f71d6ec9f6`
   - Alpha `gJ8NZZvg`
   - Sharpe 2.75
   - Fitness 1.27
   - Turnover 0.7239
   - Repair：`ts_mean(predicted_first_quantile_five_day_return_3, 2)`

3. `rplan_6d9c4e4b1e2d1fa1c0959971`
   - Alpha `QPGR2J1Q`
   - Sharpe 2.28
   - Fitness 0.88
   - Turnover 0.7809
   - Repair：`ts_mean(predicted_first_quantile_one_day_return_tech1_1, 2)`

4. `rplan_d2b6654dec5fec966b7beaf1`
   - Alpha `O0GdqlQY`
   - Sharpe 2.31
   - Fitness 0.94
   - Turnover 0.8223
   - Repair：`ts_mean(predicted_first_quantile_one_day_return_tech1_1, 2)`

5. `rplan_bbae8f1fb6250b0bcbea956f`
   - Alpha `leWw3Opx`
   - Sharpe 2.43
   - Fitness 1.00
   - Turnover 0.9028
   - Repair：`ts_mean(predicted_first_quantile_one_day_return_2, 2)`

注意：这些计划中存在“不同父 Alpha 最终 Repair expression 相同”的情况。正式执行前必须按 `sim_key` 再做去重/Cache 检查，不能机械执行所有计划。

## 10. 查看已有 PRE-TAG Check Session

```powershell
@'
import sqlite3
c=sqlite3.connect(r"ppl_runner.db"); c.row_factory=sqlite3.Row
rows=c.execute("""
SELECT alpha_id,phase,session_status,poll_count,http_request_count,
       base_gate_result,theme_gate_result,error_type,updated_at
FROM ppl_check_sessions
WHERE run_id='run_0002'
ORDER BY updated_at DESC
""").fetchall()
for r in rows:
    print(dict(r))
c.close()
'@ | python -
```

## 11. 查看指定 Alpha 已保存的 Check 结果

例如查看 `ZYEVroY0`：

```powershell
@'
import sqlite3
ALPHA='ZYEVroY0'
c=sqlite3.connect(r"ppl_runner.db"); c.row_factory=sqlite3.Row
s=c.execute("""
SELECT * FROM ppl_check_sessions
WHERE run_id='run_0002' AND alpha_id=?
ORDER BY updated_at DESC LIMIT 1
""",(ALPHA,)).fetchone()
if not s:
    print('NO_CHECK_SESSION')
else:
    print('SESSION:',dict(s))
    rows=c.execute("""
    SELECT raw_name,normalized_name,raw_result,normalized_result,
           normalized_value,normalized_limit,eligibility_outcome,
           eligibility_reason,diagnosis_outcome,diagnosis_reason
    FROM ppl_check_results
    WHERE check_session_id=?
    ORDER BY raw_name
    """,(s['check_session_id'],)).fetchall()
    for r in rows: print(dict(r))
c.close()
'@ | python -
```

修改 `ALPHA='...'` 即可查看其他 Alpha。

## 12. 对指定 Alpha 做一次 GET-only Live /check，并使用 V2.2 Parser 解析

当前 `ppl_runner.py --check-preview` 只解析 fixture，本身不访问网络。

可以先用现有 `machine_lib_V2_1.py` 做只读 GET，把真实响应保存为 fixture，再交给 V2.2 Parser。

例如 PRE-TAG 检查 `ZYEVroY0`：

```powershell
@'
import json
import machine_lib_V2_1 as m
ALPHA='ZYEVroY0'
s=m.login()
try:
    r=s.get(f"{m.BRAIN_API_URL}/alphas/{ALPHA}/check")
    r.raise_for_status()
    payload=r.json()
    envelope={
        'alpha_id':ALPHA,
        'phase':'PRE_TAG',
        'evidence_source':'MANUAL_LIVE_GET',
        **payload,
    }
    fn=f"manual_check_{ALPHA}_pretag.json"
    with open(fn,'w',encoding='utf-8') as f:
        json.dump(envelope,f,ensure_ascii=False,indent=2)
    print(fn)
finally:
    s.close()
'@ | python -
```

然后解析：

```powershell
python ppl_runner.py --check-preview --fixture .\manual_check_ZYEVroY0_pretag.json
```

注意：登录过程会有认证请求，随后只做 GET `/alphas/{id}/check`；没有 Simulation POST、PATCH、DELETE、Submit。

如果平台仍是未完成状态，不要把一次瞬时结果当最终结论，稍后重新 GET。

## 13. 查看指定 Alpha 的实时属性（Description / Tag）

只读 GET：

```powershell
@'
import json
import machine_lib_V2_1 as m
ALPHA='ZYEVroY0'
s=m.login()
try:
    payload=m.get_alpha_details(s,ALPHA)
    print(json.dumps({
        'id':payload.get('id'),
        'tags':payload.get('tags'),
        'description':(payload.get('regular') or {}).get('description'),
        'dateSubmitted':payload.get('dateSubmitted'),
    },ensure_ascii=False,indent=2))
finally:
    s.close()
'@ | python -
```

这是认证 + GET，不写 Alpha。

## 14. 生成指定 Alpha 的 Description 草稿

当前 CLI 的 Description 功能要求 fixture。

先从 Runner DB 生成 fixture。示例 `ZYEVroY0`：

```powershell
@'
import sqlite3,json
ALPHA='ZYEVroY0'
c=sqlite3.connect(r"ppl_runner.db"); c.row_factory=sqlite3.Row
cand=c.execute("SELECT * FROM ppl_candidates WHERE run_id='run_0002' AND alpha_id=?",(ALPHA,)).fetchone()
if not cand: raise SystemExit('CANDIDATE_NOT_FOUND')
field=c.execute("""
SELECT * FROM ppl_field_catalog
WHERE snapshot_id=? AND dataset_id=? AND field_id=?
LIMIT 1
""",(cand['discovery_snapshot_id'],cand['dataset_id'],cand['field_id'])).fetchone()
fixture={'candidate':dict(cand),'field_metadata':dict(field) if field else {}}
fn=f'manual_description_{ALPHA}.json'
with open(fn,'w',encoding='utf-8') as f: json.dump(fixture,f,ensure_ascii=False,indent=2)
print(fn)
c.close()
'@ | python -
```

然后：

```powershell
python ppl_runner.py --description-preview --fixture .\manual_description_ZYEVroY0.json
```

该命令不会 PATCH BRAIN。

只有 `validation_status=VALID` 的 Description 才建议人工填写。

## 15. 人工填写 Description 与 PowerPoolSelected

现有 V2.2 安全规则明确要求：

- 不自动 PATCH Description
- 不自动写 `PowerPoolSelected`
- 不自动 Submit

因此这一步在 BRAIN 网页人工完成：

1. 打开目标 Alpha。
2. 把已经核对的 Description 粘贴到 Description。
3. 人工添加 `PowerPoolSelected`。
4. 保存。

不要使用 `machine_lib_V2_1.prepare_power_pool_alpha_properties()`，因为它是 PATCH 写操作，V2.2 当前生产安全策略明确不自动使用它。

## 16. 人工修改后确认 PowerPoolSelected

使用第 13 节的实时属性 GET。

重点确认输出：

```text
PowerPoolSelected
```

出现在 `tags` 中，同时 Description 已存在且长度足够。

## 17. FINAL /check

人工属性确认后，把第 12 节里的：

```python
'phase':'PRE_TAG'
```

改为：

```python
'phase':'FINAL'
```

文件名也改成 final，例如：

```text
manual_check_ZYEVroY0_final.json
```

然后：

```powershell
python ppl_runner.py --check-preview --fixture .\manual_check_ZYEVroY0_final.json
```

只有 FINAL Gate 按当前 V2.2 Parser 判定满足要求后，才进入人工 Submit 判断。

## 18. 最终 Submit

当前系统没有自动 Submit CLI，而且 `ppl_rules.yaml` 安全规则中 `auto_submit=false`。

最终提交必须在 BRAIN 网页人工执行。

## 19. 程序中断后的恢复顺序

先查看：

```powershell
python ppl_runner.py --status --run-id run_0002
```

然后只预览 Reconcile：

```powershell
python ppl_runner.py --reconcile --run-id run_0002 --preview
```

再正式 Reconcile：

```powershell
python ppl_runner.py --reconcile --run-id run_0002
```

之后再次查看状态。

不要因为终端中断就重新 POST。同一个 `sim_key` 必须先查 `alpha_results.db` 中是否已经存在 `COMPLETE/RUNNING/SUBMITTED/UNCERTAIN_SUBMISSION`。

## 20. 重要：当前生产 Repair 没有公开 CLI

这是本次检查项目代码后最重要的发现。

当前 `ppl_runner.py` 提供了：

```text
--execute-phase10b-repair
```

但它不是 `run_0002` 的生产 Repair 命令。

原因：`ppl_engine/phase10b.py` 的 `prepare_repair_batch()` 首先调用 `validate_phase10b_config()`，明确要求：

```text
run_profile = LIVE_VALIDATION
```

而 `run_0002` 实际是：

```text
run_profile = PRODUCTION_RESEARCH
```

所以不要执行：

```powershell
python ppl_runner.py --execute-phase10b-repair --run-id run_0002 --allow-simulation-post
```

它不是正式 Production Repair 入口。

另外：当前 23 个 Repair Plan 的状态全部是：

```text
DEFERRED_INITIAL_SEARCH
```

而 Phase10B Repair 只读取：

```text
plan_status='READY'
```

当前项目也没有 CLI 用于“只把指定 Production Repair Plan 安全提升到 READY”。

结论：

当前项目的 Initial Search / Summary / Reconcile / 查询功能已经可以人工操作；但正式 `PRODUCTION_RESEARCH` Repair 缺少一个公开、安全、可选择 plan_id 的 CLI 入口。

不要用 SQL 手工把 23 个 Plan 全改成 READY 后强跑 Phase10B 命令。

## 21. 另一个重要点：HT Ratio 好候选当前没有自动 Repair Plan

当前 23 个 Repair Plan 全部来自 Simulation 后的 Local Diagnosis。

像 `ZYEVroY0`、`N1bErYe8`、`WjArW37Q` 这些已经 Local Gate PASS、但 live `/check` 被 HT Returns Ratio WARNING 卡住的 Alpha，并没有出现在当前 23 个 Repair Plan 中。

因此“修 HT Ratio”不能简单理解为直接执行当前 23 个 Deferred Plan。

当前 23 个计划主要处理：

- Turnover > 0.70
- 强负 Sharpe 的 Reverse

而不是当前 11 个 PRE-TAG 好候选的 HT Ratio 警告。

## 22. 当前最安全的人工工作流

```text
状态确认
→ Summary / Priority
→ 查看23个Repair Plan
→ 人工选少量值得修的本地失败 Alpha
→ [当前缺 Production Repair CLI，需要安全 wrapper]
→ Repair Simulation
→ Reconcile
→ Local Gate
→ GET-only PRE-TAG /check
→ V2.2 Parser
→ 选择 Finalist
→ Description Preview
→ 人工 Description + PowerPoolSelected
→ GET Alpha Details确认属性
→ GET-only FINAL /check
→ V2.2 Parser
→ 人工 Submit
```

## 23. 当前不要做的操作

不要执行：

```powershell
python ppl_runner.py --execute-phase10b-repair --run-id run_0002 --allow-simulation-post
```

不要直接 SQL：

```sql
UPDATE ppl_repair_plans SET plan_status='READY';
```

不要自动调用：

```python
prepare_power_pool_alpha_properties(...)
```

不要自动 Submit。

不要重新跑 Initial Search，当前 72/72 已完成。

## 24. 常用文件

```text
F:\一二三\v2\ppl_runner.py
F:\一二三\v2\ppl_runner.db
F:\一二三\v2\alpha_results.db
F:\一二三\v2\ppl_state.json
F:\一二三\v2\ppl_summary.json
F:\一二三\v2\ppl_execution_plan.json
F:\一二三\v2\ppl_plan.yaml
F:\一二三\v2\ppl_rules.yaml
F:\一二三\v2\machine_lib_V2_1.py
F:\一二三\v2\reports\
```

## 25. 现在建议你执行的第一条命令

只确认状态：

```powershell
Set-Location "F:\一二三\v2"
python ppl_runner.py --status --run-id run_0002
```

确认仍是 `PAUSED` 后，再执行第 7、8 节的 Repair Plan 查询。

在正式 Repair 前，应先补一个很小的 Production Repair wrapper：要求显式 `--plan-id`，默认 Preview、必须二次显式授权才允许 POST，并继续复用现有 V2.1 Simulation Adapter、Cache、Resume、Server Slot Guard 和 48 条 Repair Reserve。不要复用 Phase10B validation profile 作为生产入口。
