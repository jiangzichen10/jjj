# WorldQuant BRAIN Alpha 遍历 V2.1 — 设计与改动说明

## 目标
优先提高“可提交 Alpha”的产出效率。默认研究目标为 `POWER_POOL_ATOM`，同时保留 `ATOM / POWER_POOL / REGULAR`。

## 不动的稳定底座
- `simulation_key = expression + settings` 不变。
- 继续使用原 `alpha_results.db`，旧 COMPLETE 缓存继续命中，不重复 POST。
- 6/多路线程池、独立 Session、SQLite WAL、断点续跑、simulation_url、429 Retry-After、401/403 刷新、防重复 POST 逻辑保留。
- `vec_avg`、`vec_sum` 都保留。
- 不加入 Dataset Historical Coverage Preflight。

## 1. Data Field 筛选
- 新增 `MIN_DATA_COVERAGE`，Notebook 默认 `0.90`，可配置，不写死在流程里。
- 顺序：全部字段 → Data Coverage → 人工排除 → `MAX_FIELDS` → `prepare_fields()`。
- API 同时存在 `dateCoverage` 与 `coverage` 时，自动识别真正有信息量的 0~1 coverage 列；如果自动识别发现 coverage 类列全为 0/空，则停止并报错，避免错误过滤全部字段。
- Candidate Metadata 保存：dataset、field、type、vector_op、data_coverage、date_coverage、category、pyramid multiplier、themes 等。

## 2. Target Mode
支持：
- `POWER_POOL_ATOM`
- `ATOM`
- `POWER_POOL`
- `REGULAR`

Power Pool 支持 Region **不在本地写死**。平台实时 eligibility / submission check 是最终依据。

## 3. 并发
Notebook 配置：
- `CONCURRENCY`：用户希望开的并发数。
- `GLB_MAX_CONCURRENCY = 4`：当前 GLB 上限，可改。
- `OTHER_MAX_CONCURRENCY = 8`：当前其他 Region 上限，可改。
- 实际并发 = `min(CONCURRENCY, 当前 Region 上限)`。
- `POLL_TIMEOUT_SECONDS = 900`：默认 15 分钟。超时保留 RUNNING + simulation_url，释放 worker，后续 Resume，不重新 POST。

## 4. Stage 1 Core + Extended
Core 保留：
- raw
- rank
- zscore
- ts_mean
- ts_rank
- ts_zscore
- ts_delta
- ts_std_dev

Core 窗口以 5 / 22 / 66 为主，`ts_rank`、`ts_zscore` 不再默认铺 120。

Extended 只对 Core 已出现潜力的字段生成：
- `ts_rank(...,120)`
- `ts_zscore(...,120)`
- `ts_arg_min(...,22/66)`
- `ts_arg_max(...,22/66)`
- `ts_quantile(...,22/66/120)`

Stage1 结果增加：
- `STRICT_PASS`
- `POSITIVE`
- `FLIPPABLE_NEGATIVE`
- `EXPLORATION`
- `REJECT`
并统计 strict_rate / eligible_strict_rate。

## 5. Stage 2
- `POWER_POOL_ATOM / ATOM / POWER_POOL`：第一轮只使用 `sector / industry / subindustry`。
- `group_neutralize` 先跑。
- 如果可用候选少于设定数量，再生成 `group_rank` fallback。
- `REGULAR` 保留旧的五组结构能力。

## 6. Power Pool / ATOM 本地结构元数据
每条 Candidate 增加：
- `operator_count` / `operators`
- `data_field_count` / `data_fields`
- `dataset_count` / `dataset_ids`
- `pp_structure_ok`
- `atom_structure_ok`
- `pp_atom_structure_ok`

这些只是本地预筛；平台实时 check 是最终资格判断。

## 7. Repair
保留旧 `targeted_repair_candidates()` 作为无 live check 时的 fallback。

新增 `submission_aware_repair_candidates()`：根据 live `/check` 的失败原因，少量生成：
- LOW_SHARPE
- LOW_2Y_SHARPE
- LOW_SUB_UNIVERSE_SHARPE
- CONCENTRATED_WEIGHT / WEIGHT 类
- HIGH_TURNOVER

`ATOM / POWER_POOL_ATOM` 默认不加入可能跨 Dataset 的 region-event `trade_when`。

## 8. 数据库隔离
同一个 `alpha_results.db` 中：
- `alpha_results`：Simulation Cache，主键语义完全不变。
- `alpha_contexts`：新增研究上下文表，保存 Dataset / Target / Stage / Candidate Metadata / Strategy Metadata。

因此不同 Dataset、ATOM、Power Pool 研究可以共用同一 simulation cache，又不会在分析层混淆。

Cache hit 返回时，simulation 数值从旧 cache 取，但 Candidate/Strategy metadata 以**当前运行的 candidate**为准，避免旧 Dataset / 旧 TARGET_MODE 泄漏。

## 9. Run Guard
Stage1/Stage2/Repair 进入下一层前校验：
- candidate.dataset_id 必须等于当前 `DATASET_ID`
- target_mode 必须等于当前 `TARGET_MODE`
发现 stale/mixed candidate 直接 STOP。

## 10. 当前文件
- `machine_lib_V2_1.py`
- `Alpha遍历_V2_1.ipynb`
- 继续使用原 `alpha_results.db`

原稳定版文件不覆盖。
