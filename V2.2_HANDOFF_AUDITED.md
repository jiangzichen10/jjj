# WorldQuant BRAIN V2.2 系统审计与新会话交接

版本：V2.2 Audited Fixed 2026-08-17  
项目原路径：`F:\一二三\v2`  
正式 Run：`run_0002`  

## 0. 新聊天先看这里

这是当前项目的最终交接基线。新聊天不要从头设计系统，也不要重写现有主链。

第一原则：

- 不修改 `machine_lib_V2_1.py`。
- `alpha_results.db` 是 Simulation/Alpha Fact Truth。
- `ppl_runner.db` 是 Workflow Truth。
- `logs/ppl_v2_2.log` 是 Execution Audit Trail，不是状态真相。
- Resume-first；timeout 不等于 failure。
- 保留 SERVER SLOT GUARD、PostGate、writer lock、Repair Budget。
- 不自动 Submit、PowerPoolSelected、PATCH、DELETE。
- 真实 Simulation 必须显式单 plan 授权 `--allow-simulation-post`。
- 不对成功 Alpha 重复 Simulation。

当前最重要的真实成果：`rK2ZVL93` 已由用户人工成功提交。它是 `ZYEVroY0` 的同表达式、同 Decay=0、仅 Neutralization 从 SUBINDUSTRY 改为 MARKET 的成功对照，不要再 Repair 或重复模拟。

## 1. 当前系统基线

### 1.1 稳定底层

- 文件：`machine_lib_V2_1.py`
- SHA-256：`0F8944F696EAC8481771AE1DF87EBD2F467CF69922939B46E783944E9A794762`
- 本次审计未修改该文件。

### 1.2 数据库

`alpha_results.db`

- 完整性：`PRAGMA integrity_check = ok`
- 表数：2
- 表：`alpha_results`、`alpha_contexts`
- Schema 未修改。

`ppl_runner.db`

- 完整性：`PRAGMA integrity_check = ok`
- 表数：30
- Schema 未修改。
- `run_0002`：PAUSED / PRODUCTION_RESEARCH
- execution_hash：`ceb10b63d1643f553f60962a3b6cc740a7fd683a9ddabbd0f8ac40cc9a5edd2a`
- 当前兼容判定：`LEGACY_SCHEMA_MATCH`，execution semantics compatible。
- `run_0002.post_consumed = 63`
- Candidate 数：433
- Repair Plan 数：73
  - DEFERRED_INITIAL_SEARCH：22
  - EXECUTED：3
  - PLANNED：48
- Repair Plan committed/consumed：3 / 3
- `ppl_repairs` durable repair edge：3 条（本次审计已按现有真实历史事实回填）。

### 1.3 测试

上传版本基线：550 tests。  
本次审计新增/加强 15 个回归测试，当前收集：565 tests。

由于当前容器单次长 pytest 运行会触发环境超时，本次按覆盖域分四组运行完整测试集合：

- 141 passed
- 103 passed
- 173 passed
- 148 passed

合计：565 / 565 passed。

审计期间曾由完整测试捕获一个 Phase10A FakeMachine 兼容性回归，已修复后重新通过相关 Phase10A/10B 与全部分组测试。

## 2. 第一条完整成功 Alpha

### 2.1 Parent

Alpha：`ZYEVroY0`

Expression：

`ts_mean(predicted_first_quantile_one_day_return_2, 2)`

已知基线：

- Neutralization：SUBINDUSTRY
- Decay：0
- Sharpe：2.42
- Fitness：1.18
- Turnover：0.6249
- HT Returns Ratio：0.7374 / 0.75，未过线
- PP Corr：PASS
- Sub-universe：PASS

系统分类：`STRONG_NEAR_PASS`。

### 2.2 失败 Repair 证据

`RRmO7vba`

- 策略：HT_RATIO_TARGET_TVR，target_tvr=0.625
- Sharpe：2.28
- Fitness：1.08
- Turnover：0.6710
- HT Ratio：0.6514
- verdict：WORSE

`9qpNzjOr`

- 策略：SAME_FAMILY_MICRO_TUNE，ts_mean window 2 -> 3
- Sharpe：2.25
- Fitness：1.18
- Turnover：0.4880
- HT Ratio：0.7112
- verdict：WORSE

因此不要机械继续 target_tvr=0.60/0.65，也不要机械跑 ts_mean 4/5。

### 2.3 成功控制实验

Alpha：`rK2ZVL93`

与 Parent 保持：

- Expression 不变
- Decay=0 不变

只改变：

- Neutralization：SUBINDUSTRY -> MARKET

结果：

- Sharpe：2.15
- Fitness：1.18
- Turnover：0.6200
- Returns：0.1874
- HT Returns Ratio：0.7732 / 0.75，PASS
- Sub-universe：1.66 / 1.32，PASS
- Classification：High Turnover、Investable High Turnover
- 用户已人工成功提交。

控制结果：HT Ratio `0.7374 -> 0.7732`，delta `+0.0358`。这是该 signal family 的真实成功证据，但不能推广成“MARKET 对所有 Alpha 都更好”。

另有：

- `WjAv0g6Z`：INDUSTRY + Decay1，Sharpe2.37，Fitness1.23，Turnover0.6223，HT0.7450。
- `e73XkLKl`：MARKET + Decay2，Sharpe2.03，Fitness1.16，Turnover0.5305，HT0.7634。

同为 MARKET 时，Decay0 的 `rK2ZVL93` 比 Decay2 的 `e73XkLKl` 更优，因此当前成功样本以 `rK2ZVL93` 为准。

## 3. 本次审计发现并修复的问题

### P0-01：Neutralization Repair 的 Preview Settings 与真实 POST Settings 可能不一致

原问题：V2.2 用 MARKET 计算新 sim_key，但调用 V2.1 时仍可能使用 run-level SUBINDUSTRY，存在“Preview 是 MARKET、实际 POST 是 SUBINDUSTRY”的严重身份/缓存风险。

修复：

- V2.2 repair candidate 携带 durable effective settings。
- `simulation_adapter.execute_with_v21` 按 candidate effective settings 分组执行。
- 真正进入 V2.1 前重建 settings + sim_key，必须与 previewed sim_key 完全一致，否则硬阻断 `V21_SETTINGS_SIM_KEY_MISMATCH`。
- 不修改 V2.1。

### P0-02：Settings Micro Tune 可能重置 Parent 的其它 Settings

原问题：child materialization 从 run config 开始，而不是 Parent 实际 `settings_json`，改 Neutralization 时可能顺带把 Parent 的 Decay/Truncation 重置。

修复：先复制 Parent durable settings，再只叠加 requested override。现在 Neutralization 微调真正做到“只改 Neutralization”。

### P0-03：Candidate 行的 neutralization 与 settings_json 可能不一致

修复：child row 的 neutralization 使用最终 effective settings，同 sim_key/settings 保持一致。

### P1-01：CACHE_COMPLETE / RESUME_EXISTING Repair 原来没有真正闭环

原问题：零 POST 的 Cache/Resume plan 可能直接标 EXECUTED，而没有完整 materialize/link/resume/evaluate。

修复：

- CACHE_COMPLETE：恢复事实、同步 child、Local Gate、必要时 PRE-TAG Check；零 POST。
- RESUME_EXISTING：真正委托 V2.1 resume/poll，不重新 POST；完成后再 EXECUTED。
- 仍 RUNNING/SUBMITTED 时保持 READY，不伪装完成。

### P1-02：`ppl_repairs` durable lineage 为空

原问题：3 条已执行 Repair 没有写入 many-to-one repair edge，后续证据学习无法可靠还原 Parent -> Child。

修复：Production Repair 统一写 `ppl_repairs`；复用 Candidate 时不再破坏原 primary lineage，而是追加 repair edge/provenance link。现有 run_0002 历史 3 条已按真实 DB 事实回填。

### P1-03：Near-Pass stop rule 没有使用真实系统 Repair Outcome

修复：`preview_rescue`/context 读取 `ppl_repairs.side_effect_verdict`，系统 confirmed TARGET_PASS/WORSE 真正参与 stop 规则；外部 evidence 仍不能驱动系统 stop。

### P1-04：External Evidence 可能错误影响不相关 Candidate

修复：外部证据按 target failure + parent expression + signal family 过滤。一次 family 成功只能作为相同上下文的 priority hint，不作为全局规律。

### P1-05：Rescue Preview 使用 run-level Neutralization，而非 Candidate 实际 Settings

修复：优先读取 candidate `settings_json`。不会再对已经 MARKET 的 Candidate 推荐 MARKET。

### P1-06：Repair parameter_change 摘要可能显示 `None -> MARKET`

修复：摘要从 Parent durable settings_json 比较，正确显示如 `SUBINDUSTRY -> MARKET`。

### P1-07：PLANNED 但没执行的 Plan 被当成“已经试过”

修复：只有 EXECUTED 或真正 consumed 的 Repair 才算 empirical attempt。PLANNED 不会降低策略优先级。

### P1-08：Manual Review 过早升级 / 系统成功后仍可能进入人工队列

修复：

- 自动 Rescue 尚有机会时，不给 P1/P2/P3。
- 只有 attempt exhausted 或明确整体 stop 才升级。
- SYSTEM TARGET_PASS 永远不会因 attempt count 又进入 P1 Manual。
- `repairs_attempted` 只列真正执行过的 Repair。

### P1-09：Rescue Target 可能来自旧历史 Plan，而不是当前 blocker

修复：以当前 PRE-TAG check 的 primary blocker 决定 target_failure / rescue_target。

### P1-10：HTTP retry 可能被错误按多次 Repair Budget 消耗

修复：Budget 以 logical unique sim_key 为单位；HTTP retry 只保留 `post_attempted` 审计，不重复消耗 Repair Reserve。

### P1-11：Production Repair 报告 lifecycle 是执行前旧快照

该问题曾真实表现为 child 已推进，但 report 仍显示 `PLANNED`。

修复：最终 report 重新读取 durable current Candidate，输出当前 `lifecycle` 和 `candidate_simulation_status`。

### P2-01：Audit 查询返回“最早 N 条反转”，不是“最近 N 条”

修复：流式 bounded deque，返回最新匹配记录，newest first。

### P2-02：`--show-audit-log` 会先给日志自己追加 RUN_START

修复：Audit 查询现在 observationally read-only，不因查看日志而写 LOGGING_INITIALIZED/RUN_START。

### P2-03：UNRESOLVED threshold 仍生成误导 raw_gap

修复：direction unresolved 时 raw_gap/normalized_gap 都保持 None，不参与自动排名。

### P2-04：STRONG_NEAR_PASS 计算 Fitness 但没有真正要求 Fitness pass

修复：Live check 优先；legacy/synthetic 缺 Fitness 时用可配置 fallback。Strong Near-Pass 必须保护基本质量。

### P2-05：Near-Pass Rescue 参数只在 Python 默认值里，没有显式落到项目规则文件

修复：`ppl_rules.yaml` 已新增 `near_pass:` 区块，包含 blocker/gap/attempt/correlation/neutralization candidate pool。该区块不进入 execution_hash material，因此 run_0002 仍保持 LEGACY_SCHEMA_MATCH。

### P2-06：成功外部 evidence 原来使用了错误的 e73XkLKl 对照

原记录把 `e73XkLKl` 当成“只改 Neutralization”的成功样本，但它同时是 MARKET + Decay2。

修复：`rescue_evidence.json` 更新为干净控制组 `rK2ZVL93`：SUBINDUSTRY+Decay0 -> MARKET+Decay0。

### P2-07：审计修复引入 Phase10A FakeMachine 回归

完整测试过程中发现 `_v21_candidate` 的新 sim-key guard 会要求 Phase10A 测试 FakeMachine 具备 `build_settings`。

修复：只有携带 durable `_v22_settings` 的 V2.2 per-candidate settings override 才执行 hard sim-key validation；legacy/validation candidates 继续使用原 V2.1 contract。相关 Phase10A/10B 测试全部通过。

## 4. Logging 当前状态

主日志：`logs/ppl_v2_2.log`，JSONL + rotating file。

日志职责：解释“为什么发生”，不作为状态真相。

已经覆盖：

- cache/reuse decision
- budget check / consumed
- simulation POST attempt/success/uncertain
- resume
- simulation complete
- local gate
- PRE-TAG check
- repair plan/execution/outcome
- near-pass/rescue/manual-review
- state transition
- HTTP 429/retry/timeout/error

历史日志中曾有一条旧 bug 产生的 `ZYEVroY0 STOP_RESCUE_SUCCESS`。本次没有删除历史审计，而是追加 `AUDIT_CORRECTION`，明确该事件来自 external-evidence trust 修复之前，当前系统不把外部 evidence 当系统成功事实。

## 5. 当前 Near-Pass 状态

当前 DB replay：

- STRONG_NEAR_PASS：1
  - `ZYEVroY0`，HT gap normalized 0.0168
- NEAR_PASS：1
  - `9qpNzjOr`，HT gap normalized 0.051733
- Manual Review：P1=0，P2=0，P3=1

`ZYEVroY0` 当前 preview：

- attempts_used=2
- attempts_remaining=3
- 外部/用户确认成功 Alpha：`rK2ZVL93`
- recommended action：`VERIFY_EXTERNAL_ALPHA_BEFORE_SIMULATION`
- `allowed_to_execute=false`

这是一项预算保护：系统知道有用户已确认的现存成功 Alpha，因此禁止为 ZY 再重复打一条 MARKET Simulation。但因为 `rK2ZVL93` 没有通过 V2.2 的 DB reconcile 进入 system fact，系统也不会伪造 `STOP_RESCUE_SUCCESS`。

## 6. 仍保留的已知限制（不是当前 P0 blocker）

### 6.1 `rK2ZVL93` 仍是 USER_CONFIRMED_EVIDENCE，不是 V2.2 DB system fact

用户已成功提交，但它是人工在 BRAIN 操作得到，不在 run_0002 的 candidate/check durable chain 中。

后续如需要，可做一个严格 GET-only 的 Manual Alpha Reconcile：输入 Alpha ID，验证 expression/settings/check 后回流 Candidate/Check Fact。不要重新 Simulation。

### 6.2 Rescue Preview 目前没有统一的 `--plan-rescue` 持久化 CLI

系统已经能推荐 `NEUTRALIZATION_MICRO_TUNE`，也有 planning primitive，但当前公开 CLI 主要是 `--preview-rescue`、`--plan-check-derived`、现有 formal Production Repair plan。

这是可用性缺口，不是安全 blocker。下次开发时如要补，只做“单 candidate、单推荐、planning-only、幂等”，不要做 execute-all。

### 6.3 SingleRunnerLock 不自动删除 stale lock

当前设计是保守 fail-closed：存在 lock 就阻止 writer。进程异常退出后可能需要人工确认 PID 已死亡，再删除 `ppl_runner.lock`。

这是有意优先数据安全。不要贸然做自动 stale-delete，除非能够可靠验证 same-host dead PID。

### 6.4 Theme / live limit 是时间敏感事实

当前项目针对 Aug 2026 Power Pool Theme 的 preset/live check。未来 Theme 更换时应以 BRAIN live `/check` 为最终权威，不要沿用旧 0.75 等阈值作为硬编码事实。

## 7. 推荐运行方式

进入项目：

```cmd
cd /d "F:\一二三\v2"
```

查看 Near-Pass：

```cmd
python ppl_runner.py --list-near-pass --run-id run_0002
```

查看人工复核队列：

```cmd
python ppl_runner.py --list-manual-review --run-id run_0002
```

查看某 Candidate Rescue Preview：

```cmd
python ppl_runner.py --preview-rescue --run-id run_0002 --candidate-id <candidate_id>
```

列 Repair Plans：

```cmd
python ppl_runner.py --list-repair-plans --run-id run_0002 --plan-source all
```

单 Plan Production Preview（不 POST）：

```cmd
python ppl_runner.py --production-repair --run-id run_0002 --plan-id <repair_plan_id>
```

只有明确确认 Preview 后，才真实执行：

```cmd
python ppl_runner.py --production-repair --run-id run_0002 --plan-id <repair_plan_id> --allow-simulation-post
```

查看日志：

```cmd
python ppl_runner.py --show-audit-log --run-id run_0002 --audit-limit 50
```

按 Plan 查完整链：

```cmd
python ppl_runner.py --show-audit-log --run-id run_0002 --audit-plan-id <repair_plan_id> --audit-limit 100
```

## 8. 下一阶段建议

不要继续“开发系统”优先。当前应进入少开发、多运行阶段。

建议顺序：

1. 保护已提交 `rK2ZVL93`，不再改参数、不重复 Simulation。
2. `--list-near-pass` 找下一条真正值得抢救的 Strong Near-Pass。
3. 对 HT Ratio 小 gap 的强 Alpha，优先 Settings Micro Tune，尤其先检查已有 Neutralization/Decay 历史 Alpha 和 Cache。
4. 一次只做一个 Canary，真实 POST 前必须 Preview。
5. 成功立即 STOP/PROTECT；失败记录 system evidence 再决定下一枪。
6. 自动 Rescue 耗尽后才进入 P1/P2 Manual Review，留给用户亲自看。

## 9. 新聊天推荐首条提示词

把本文件和审计修正版 ZIP 上传到新聊天，然后发：

> 这是我的 WorldQuant BRAIN V2.2 Alpha 自动遍历/Power Pool 系统。请先完整阅读 `V2.2_HANDOFF_AUDITED.md`，不要重构，不要修改 `machine_lib_V2_1.py`，不要自动 POST。当前正式 run 是 `run_0002`。`rK2ZVL93` 已经人工成功提交，要保护并避免重复模拟。先核对交接文档里的安全约束和当前 DB 状态，然后从 `--list-near-pass` 的结果继续找下一条最值得 Rescue 的 Alpha。所有平台规则、live limit、Check 语义以现有持久化 live facts 和 BRAIN 实际 `/check` 为准；不确定时不要猜。

## 10. 安全交付说明

本次审计修正版 ZIP 会主动排除：

- `key.txt`
- `*.db-wal`
- `*.db-shm`
- `.pytest_cache`
- `__pycache__`
- `.pyc`
- agent 临时目录/记忆目录
- stale `ppl_runner.lock`（如存在）

数据库使用 SQLite backup API 生成一致性快照后再打包。

解压到本机后，需要把你自己的真实 `key.txt` 放回项目根目录；不要把真实凭据继续分享给第三方或模型。
